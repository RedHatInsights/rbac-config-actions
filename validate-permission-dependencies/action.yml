name: Custom Validation
description: Additional validation not covered in the schema validation
inputs:
  permissions_path_pattern:
    description: Where the permissions live
    required: true
runs:
  using: node16
  main: index.js
